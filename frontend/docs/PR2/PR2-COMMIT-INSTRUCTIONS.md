# PR 2 Commit Instructions

## Suggested branch

```bash
git checkout -b feature/pr2-architecture-folders
```

## Suggested commits

```bash
git add src/core src/features src/lib src/common src/front-types-domain
git commit -m "chore(architecture): add frontend folder foundation"
```

```bash
git add docs/PR2
git commit -m "docs(migration): document PR2 architecture foundation"
```

## PR title

```txt
chore(architecture): add frontend folder foundation
```

## PR notes

- Mention that no runtime behavior changed.
- Mention that type migration is intentionally deferred.
- Include validation command results.
